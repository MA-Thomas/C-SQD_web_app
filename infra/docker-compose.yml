services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "55432:5432"
    environment:
      POSTGRES_USER: csqd
      POSTGRES_PASSWORD: csqd
      POSTGRES_DB: csqd
    volumes:
      - csqd_postgres_data:/var/lib/postgresql/data

volumes:
  csqd_postgres_data:
