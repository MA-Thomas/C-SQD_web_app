[package]
name = "csqd-identity"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
csqd-domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
chrono = "0.4"
serde_json = "1"
