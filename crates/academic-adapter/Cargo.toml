[package]
name = "csqd-academic-adapter"
edition.workspace = true
license.workspace = true
version.workspace = true

[dependencies]
# The adapter is a lens over the FEN substrate: it may depend on the domain
# crate, never the reverse.
csqd-domain = { path = "../domain" }
serde = { version = "1", features = ["derive"] }
